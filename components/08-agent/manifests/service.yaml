apiVersion: v1
kind: Service
metadata:
  name: agent-service
  namespace: ${NS_SERVICES}
  labels:
    app: agent-service
spec:
  type: ClusterIP
  selector:
    app: agent-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
